#include "main.h"
#include <stdio.h>
#include <unistd.h>

/**
 * add - function that adds two integers and returns the result.
 * @a: first operande
 * @b: second operand
 * Return: result
 */

int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
