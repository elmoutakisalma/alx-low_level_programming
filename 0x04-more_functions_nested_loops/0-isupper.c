#include "main.h"

/*
 * _isupper - check if a letter is upper
 * @c: verify if it s an uppercasa
 * Return: 0 or 1
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
